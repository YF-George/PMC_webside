/** @type {import('tailwindcss').Config} */
export default {
    content: ['./src/**/*.{html,js,svelte,ts}'],
    theme: {
        extend: {},  // 可以在這裡擴展主題設定
    },
    plugins: [],  // 可以在這裡添加 Tailwind CSS 插件
};
